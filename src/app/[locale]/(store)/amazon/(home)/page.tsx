import HeroCarousel, {
  HomeCarousel,
} from '@/components/amazon/hero/hero-carousel'

function page() {
  return (
    <div className="flex">
      <HeroCarousel />
    </div>
  )
}

export default page
